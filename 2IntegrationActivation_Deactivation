pipeline {
    agent any

    stages {
        stage('Deactivate Integration') {
            steps {
                // Compile the Java code
                sh 'javac DeactivateIntegration.java'

                // Run the Java code
                sh 'java DeactivateIntegration'
            }
        }
    }
}
